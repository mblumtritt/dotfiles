#! /bin/sh

case "$1" in
(-h|--help) echo "Usage: ${0##*/}

Convert MARKDOWN from stdin into plain TEXT to stdout." ;;
('') set -e; exec pandoc --from=gfm --to=plain "$@" ;;
(*) echo "${0##*/}: invalid argument - $1" >&2 ; exit 1 ;;
esac
