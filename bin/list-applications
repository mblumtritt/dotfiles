#! /bin/sh

case "$1" in
(-h|--help) echo "Usage: ${0##*/}

List downloaded apps from AppStore." ;;
('') set -e; exec mdfind kMDItemAppStoreHasReceipt=1 ;;
(*) echo "${0##*/}: invalid argument - $1" >&2 ; exit 1 ;;
esac
