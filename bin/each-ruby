#! /bin/sh

set -e

[ "$1" = '--help' ] && echo "Usage: ${0##*/}

Execute given command for each installed ruby version." && exit

for ruby in $(asdf list ruby | grep -oE '[0-9]+\.[0-9]+\.[0-9]+'); do
	echo "• Ruby $ruby"
	ASDF_RUBY_VERSION="$ruby" asdf exec $@
done
