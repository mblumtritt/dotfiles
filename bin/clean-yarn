#! /bin/sh
USAGE="Usage: ${0##*/}

Clean up YARN cache."

set -e
case "$1" in
('') yarn cache clean --force ;;
(-h|--help) echo "$USAGE" ;;
(*) echo "${0##*/}: invalid argument - $1" >&2 ; exit 1 ;;
esac
