#! /bin/sh
USAGE="Usage: ${0##*/} [CITY]

Display the meteogram of a city - uses Leipzig as default."

set -e
case "$1" in
(-h|--help) echo "$USAGE" ;;
('') finger leipzig@graph.no ;;
(*) finger "${1}@graph.no" ;;
esac
