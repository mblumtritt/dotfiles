#! /bin/sh

set -e

[ "$1" = '--help' ] && echo "Usage: ${0##*/}

Convert YAML from stdin into JSON to stdout." && exit

ruby --disable-all -rpsych -roj -e'Oj.to_stream STDOUT,Psych.load_stream(STDIN)'
