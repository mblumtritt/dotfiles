#! /bin/sh
USAGE="Usage: ${0##*/} URL

Display a file from given URL."

set -e
case "$1" in
('') echo "${0##*/}: URL missing" >&2 ; exit 1 ;;
(-h|--help) echo "$USAGE" ;;
(*) exec curl \
  --fail \
  --compressed \
  --silent \
  --location \
  "$@"
;;
esac
