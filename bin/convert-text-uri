#! /bin/sh

set -e

[ "$1" = '--help' ] && echo "Usage: ${0##*/}

Convert TEXT from stdin into URI (WWW form component) to stdout." && exit

ruby --disable-all -ruri -e'puts URI.encode_www_form_component STDIN.readlines(chomp:true).join'
