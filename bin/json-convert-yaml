#! /usr/bin/env ruby
# frozen_string_literal: true

require 'yaml'
require 'oj'

begin
  YAML.dump(Oj.load($stdin.read), $stdout)
rescue Oj::ParseError => e
  warn(e) or exit!
rescue Interrupt
  exit(130)
end
