#! /bin/sh

set -e

if [ "$1" = '--help' ]; then echo "Usage: ${0##*/}

Scan WLAN and print information.
"
else
	/System/Library/PrivateFrameworks/Apple80211.framework/Versions/A/Resources/airport -s
fi
