#! /bin/bash

set -e

if [ "$1" = '--help' ]; then echo "Usage: ${0##*/} NAME

Get secret by NAME.
"
elif [ $# = 0 ]; then
	(>&2 echo "${0##*/}: NAME missing")
	exit 1
else
	pass show "secrets/$@"
fi
