#! /bin/sh
USAGE="Usage: ${0##*/}

List all opened ports and related processes."

set -e
case "$1" in
('') lsof -i -P | grep LISTEN ;;
(-h|--help) echo "$USAGE" ;;
(*) echo "${0##*/}: invalid argument - $1" >&2 ; exit 1 ;;
esac
