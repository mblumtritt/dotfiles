#! /bin/sh

[ "$1" = '--help' ] && echo "Usage: ${0##*/}

Clean up Docker stuff." && exit

docker ps >/dev/null 2>&1 || open --background -a Docker

docker image prune -f
docker container prune -f
docker volume prune -f
docker network prune -f
docker system prune -f -a
