#! /bin/sh
USAGE="Usage: ${0##*/}

Update Ruby gems."

set -e
case "$1" in
('')
  gem update --system --no-document
  gem update --no-document
;;
(-h|--help) echo "$USAGE" ;;
(*) echo "${0##*/}: invalid argument - $1" >&2 && exit 1 ;;
esac
