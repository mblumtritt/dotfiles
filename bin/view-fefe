#! /bin/sh
USAGE="Usage: ${0##*/}

Displays Fefes blog."

set -e
case "$1" in
('') fetch-web https://blog.fefe.de | convert-html-text | less ;;
(-h|--help) echo "$USAGE" ;;
(*) echo "${0##*/}: invalid argument - $1" >&2 ; exit 1 ;;
esac
