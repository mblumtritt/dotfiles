#! /bin/sh

set -e

[ "$1" = '--help' ] && echo "Usage: ${0##*/} CSV_FILE

View given CSV_FILE." && exit

[ $# = 0 ] && (>&2 echo "${0##*/}: CSV_FILE missing") && exit 1

sed 's/,,/, ,/g;s/,,/, ,/g' "$@" | column -s, -t | less -#2 -N -S
