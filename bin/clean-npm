#! /bin/sh
USAGE="Usage: ${0##*/}

Clean up NPM cache."

set -e
case "$1" in
('') npm cache clean --force ;;
(-h|--help) echo "$USAGE" ;;
(*) echo "${0##*/}: invalid argument - $1" >&2 && exit 1 ;;
esac
