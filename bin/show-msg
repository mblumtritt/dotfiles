#! /bin/sh

set -e

[ "$1" = '--help' ] && echo "Usage: ${0##*/} MESSAGE [TITLE]

Display a notification message." && exit

[ $# = 0 ] && (>&2 echo "${0##*/}: MESSAGE missing") && exit 1

terminal-notifier -message "$1" -title "${2-Notification}" -ignoreDnD
