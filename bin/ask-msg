#! /bin/sh

set -e

[ "$1" = '--help' ] &&
echo "Usage: ${0##*/} MESSAGE [TITLE]

Request a user action." && exit

[ $# = 0 ] && (>&2 echo "${0##*/}: MESSAGE missing") && exit 1

osascript -e "tell app \"System Events\" to display dialog \"$1\" with title \"${2-Notification}\"" > /dev/null 2>&1
