#! /bin/bash

set -e

[ "$1" = '--help' ] && echo "Usage: ${0##*/}

View colors." && exit

CLEAR=$(tput op)
COLS=$(tput cols)

for i in {0..255} ; do
	printf "$(tput setaf "$i" ; tput setab "$i") %03d $CLEAR" "$i"
	{ test "$(((i + 1) % (COLS / 5)))" -eq '0' || test "$i" -eq '255'; } && echo
done
