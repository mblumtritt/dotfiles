#! /bin/sh
USAGE="Usage: ${0##*/}

Update Homebrew and Homebrew bundles."

set -e
case "$1" in
('')
  brew update
  brew bundle --global
  brew upgrade
  brew upgrade --cask
;;
(-h|--help) echo "$USAGE" ;;
(*) echo "${0##*/}: invalid argument - $1" >&2 && exit 1 ;;
esac
