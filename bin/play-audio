#! /bin/sh
USAGE="Usage: ${0##*/} FILE

Plays given audio FILE."

set -e
case "$1" in
('') echo "${0##*/}: FILE missing" >&2 ; exit 1 ;;
(-h|--help) echo "$USAGE" ;;
(*) exec afplay -q 1 "$@" ;;
esac
